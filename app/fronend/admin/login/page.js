import React from 'react'

const Login = () => {
  return (
    <div>Login</div>
  )
}

export default Login


// 1. Admin Login
// Page: /admin/login

// Validate with hardcoded email/pass or next-auth + MongoDB user

// On success → JWT or session → redirect to dashboard